import { Meta } from '@storybook/addon-docs'
import utils from './utils'

<Meta title="Tokens/Elevation" />

## Elevation Tokens

### Default

<table>
  <tbody>
    {Object.entries(utils.defaultElevation).map(([key, value]) => (
      <tr key={key}>
        <td>
          <code>{`$elevation.${key}`}</code>
        </td>
        <td style={{ padding: '20px' }}>
          <div
            style={{
              width: '100px',
              height: '100px',
              backgroundColor: '#fff',
              borderRadius: '8px',
              boxShadow: value,
            }}
          />
        </td>
        <td style={{ padding: '20px' }}>{`box-shadow: ${value};`}</td>
      </tr>
    ))}
  </tbody>
</table>
