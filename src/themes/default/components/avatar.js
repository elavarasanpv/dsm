const baseStyle = {
  display: 'flex',
  justifyContent: 'center',
  alignItems: 'center',
  position: 'relative',
  border: '1px solid transparent',
}

const activeContainer = {
  justifyContent: 'center',
  display: 'flex',
  alignItems: 'center',
  background: '#fff',
  borderRadius: '50%',
  position: 'absolute',
  border: '1px solid #fff',
}

export default {
  baseStyle,
  activeContainer,
}
